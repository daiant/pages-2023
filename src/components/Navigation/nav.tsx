import styles from './nav.module.scss';

export default function Nav() {
    return (
        <nav className={styles.nav}>
            <p>Carlos is</p>
            <img src="/burgir.svg" />
        </nav>

    )
}